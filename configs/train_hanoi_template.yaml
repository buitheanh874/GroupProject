scenario_calibration: configs/hanoi_calibration.yaml
force_calibration_overrides: false

run:
  seed: 42
  run_name: train_hanoi

logging:
  log_dir: logs
  model_dir: models
  results_dir: results

train:
  episodes: 100
  route_pool_manifest: train/manifest.txt

env:
  sumo:
    net_file: networks/hanoi/net.xml
    use_pcu_weighted_wait: true
    use_enhanced_reward: false
    reward_exponent: 1.0
    normalize_state: true
    cycle_options_sec: [60, 90, 120]
    action_splits:
      - [0.30, 0.70]
      - [0.40, 0.60]
      - [0.50, 0.50]
      - [0.60, 0.40]
      - [0.70, 0.30]

action_table: []

normalization:
  mean: [0.0, 0.0, 0.0, 0.0]
  std: [1.0, 1.0, 1.0, 1.0]
