# GroupProject

Supports SUMO single-TLS (legacy) and multi-TLS hub-and-spoke with shared Dueling DQN. See `docs/README.md` for config keys and `configs/train_hub_spoke_demo.yaml` for the 12D state/action-table sample. Multi-node assets go under `networks/hub_spoke/`.
